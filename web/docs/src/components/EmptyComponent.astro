---
// Empty placeholder used to suppress Starlight's built-in ThemeSelect.
// Story 2.9 Decision #6: dark-only V1 (light theme deferred to V2 backlog).
// Showing a non-functional toggle confused users — this removes it from the header.
// Future: when light mode lands (V2), revert to Starlight default by removing the
// `components.ThemeSelect` override in astro.config.mjs.
---
