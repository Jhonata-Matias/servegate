---
title: Welcome
description: servegate is the open-source HTTP gateway for FLUX.1-schnell text-to-image, Qwen-Image-Edit single- and multi-image edits, and Gemma 4 text generation. One authenticated endpoint, async submit/poll for images, streaming SSE for text.
template: doc
---

import { LinkCard, CardGrid, Badge } from '@astrojs/starlight/components';

<Badge text="Alpha" variant="caution" /> servegate is the canonical HTTP contract for generating images with **FLUX.1-schnell**, editing images with **Qwen-Image-Edit** (single- or multi-image), and generating text with **Gemma 4**. Images use async submit/poll; text uses streaming SSE on the same authenticated gateway.

Latest server: `gemma4-flux-serverless:0.2.1` (Qwen-Image-Edit 2509 with multi-image support).
Latest SDK: `@jhonata-matias/flux-client@0.4.0`.

## Get started

<CardGrid stagger>
	<LinkCard
		title="🚀 Quickstart"
		description="Make your first authenticated image job and learn where text generation fits next."
		href="/quickstart/"
	/>

	<LinkCard
		title="🖼 Generate images"
		description="Create a new image from a text prompt, then poll until output.image_b64 is ready."
		href="/generate-images/"
	/>

	<LinkCard
		title="🎨 Edit images"
		description="Change one or two reference images with Qwen-Image-Edit and a text instruction."
		href="/edit-images/"
	/>

	<LinkCard
		title="💬 Generate text"
		description="Call Gemma 4 through POST /v1/generate with streaming SSE or one JSON response."
		href="/generate-text/"
	/>

	<LinkCard
		title="📖 API Reference"
		description="Use the exact HTTP contracts after you know which capability you need."
		href="/api/"
	/>

	<LinkCard
		title="⚠ Handle errors"
		description="Map gateway responses to recovery actions and SDK error classes."
		href="/errors/"
	/>

	<LinkCard
		title="🛠 TypeScript SDK"
		description="@jhonata-matias/flux-client wraps polling, validation, and typed errors."
		href="/sdk/"
	/>
</CardGrid>

Need access first? Request alpha access from the [GitHub repo issue template](https://github.com/Jhonata-Matias/servegate#alpha-access). For release notes, see [GitHub releases](https://github.com/Jhonata-Matias/servegate/releases). For operational evidence, read the [RunPod Video Report](/runpod-video-report/).

## Why servegate

- **Async-first.** Submit/poll lets long inferences (cold start ~130s, warm ~7s) coexist with short ones behind one authenticated edge.
- **Three modalities, one credential.** Same API key proxies T2I (FLUX.1-schnell), i2i (Qwen-Image-Edit single/multi), and text (Gemma 4 via vLLM).
- **Open source.** Apache 2.0 / MIT, alpha-public on GitHub.
- **Cost-aware.** RunPod Serverless behind Cloudflare Workers; near-zero idle.
