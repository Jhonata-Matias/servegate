# CodeRabbit Pre-PR Scan — Story 4.2 Round 2

**Date:** 2026-04-25 (after commit f87bc13)
**Scope:** committed changes vs main (`coderabbit --prompt-only -t committed --base main`)
**Result:** 7 findings (0 CRITICAL, 0 HIGH after contextual review, 4 MEDIUM, 3 LOW)
**Duration:** ~90s (well under 30min timeout)

## Findings (raw → severity-classified)

| # | File | Lines | CR Type | Severity (QA assess) | Issue summary |
|---|---|---|---|---|---|
| F1 | `gateway/src/runpod-text.ts` | 19-21 | potential_issue | **MEDIUM** | No `RUNPOD_API_KEY` validation; if env missing, sends `Bearer undefined`. Defensive. |
| F2 | `gateway/src/rate-limit.ts` | 139-170 | potential_issue | **MEDIUM** (naming, NOT defect) | `checkTokenBudgetAndReserve` reads but does NOT write reservation. Function name misleading vs design. |
| F3 | `docs/usage/text-gen-api-quickstart.md` | 13 | potential_issue | MEDIUM | Auth header inconsistency: streaming uses `X-API-Key`, non-streaming uses `Authorization: Bearer`. |
| F4 | `gateway/tests/rate-limit-tokens.test.ts` | 32-39 | potential_issue | LOW | "allows when estimate fits" doesn't assert `kv.put` — moot if F2 is renamed (no reservation expected). |
| F5 | `gateway/src/generate.ts` | 277-284 | potential_issue | MEDIUM | `recordAsync` swallows errors when no ctx (Round 1 L2 escalated). |
| F6 | `docs/api/reference.md` | 437-455 | refactor_suggestion | LOW | Missing "Text Generation Limits" subsection under Rate Limiting. |
| F7 | `gateway/tests/generate.test.ts` | 178-185 | potential_issue | LOW | CORS preflight test missing ctx argument (consistency). |

## Severity reasoning

### F1 — MEDIUM (not HIGH)
- Production: env vars set via `wrangler secret` — never missing
- `Env` interface marks `RUNPOD_API_KEY: string` as required (no `?:`)
- Worst case: 401 from RunPod → gateway 502 graceful
- Fix is trivial (~5 lines); recommend before deploy as defensive

### F2 — MEDIUM (naming/clarity, NOT a real defect)
**Critical context CodeRabbit missed:** `gemma-gateway-decision.md §5` explicitly designs pre-flight as APPROXIMATE check (`len(body)/4 + max_tokens` estimate), with EXACT accounting via `recordTokenUsage` post-flight in `ctx.waitUntil`. The function "doesn't reserve" by design — it's the same KV eventual-consistency tradeoff as Epic 2 Story 2.5 R7 (image rate-limit). Overshoot ≤10% is documented as accepted.

**Recommendation:** rename `checkTokenBudgetAndReserve` → `checkTokenBudget` (drop "AndReserve" — misleading) + add inline comment referencing gateway-decision §5. Alternatively keep name + add comment. NOT a defect.

### F5 — MEDIUM (Round 1 L2 escalated)
Round 1 noted as LOW because production path always has ctx. CodeRabbit highlights it again — escalating to MEDIUM as defensive logging is cheap and improves observability of edge cases.

### Others
F3, F4, F6, F7 are documentation/test-consistency improvements. None block functionality.
