# Quality Gate — Story 2.9 Refactor Landing into Astro Starlight Docs Portal
# Generated by Quinn (@qa) — 2026-04-29

storyId: "2.9"
storyTitle: "Refactor Landing into Astro Starlight Docs Portal (3-column docs experience replacing single-page marketing landing)"
storyPath: docs/stories/2.9.docs-portal-refactor.story.md
epic: "Epic 2 — Consumer Integration"
prd_version: "v1.0"

review:
  date: 2026-04-29
  reviewer: "Quinn (Test Architect)"
  type: "static review — build + linkinator + Pagefind grep + content traceability + CodeRabbit committed"
  scope:
    - astro_build_clean
    - linkinator_zero_broken_links
    - pagefind_search_terms_grep
    - multi_image_semantic_accuracy_vs_sdk
    - security_no_hardcoded_creds
    - coderabbit_committed_against_main
    - ac_traceability_10_of_10
  out_of_scope_this_review:
    - "AC7 (Lighthouse ≥ 90 each page) — requires browser env; defer to @qa post-PR against Vercel preview"
    - "AC8 (WCAG AA via axe) — same browser dependency; defer post-PR"
    - "AC10 (Vercel cutover validation) — Phase 4 @devops scope, not part of this dev run"
    - "Visual parity / responsive / sidebar click-through — deferred to @ux-design-expert preview review pre-cutover"

verdict: CONCERNS
quality_score: 88
expires: 2026-05-13  # 14-day gate window

verdict_history:
  - date: 2026-04-29
    verdict: CONCERNS
    score: 88
    rationale: |
      No CRITICAL or HIGH findings. Static checks all pass: build clean (6 pages, ~4s),
      linkinator zero broken (16 internal links), Pagefind index built, multi-image i2i
      claims semantically verified against sdk/src/types.ts and sdk/CHANGELOG.md
      (image2, input_image_b64_2, all 5 _2-suffixed metadata fields, field discriminator).
      Brand palette correctly extracted from web/landing/index.html (blue accent,
      Inter+JetBrains Mono — fixing the spec's incorrect teal+Geist assumption).
      CONCERNS verdict driven by:
      (1) AC7 (Lighthouse) and AC8 (WCAG AA) explicitly deferred per Dev Completion Notes
          due to no browser env in main session — must complete pre-merge against Vercel preview.
      (2) 5 LOW-severity polish observations from CodeRabbit (none blocking; documented below).
      Story is structurally complete for a docs-portal refactor; remaining concerns are
      standard browser-mediated AC verifications that always happen post-PR.

acceptance_criteria:
  AC1:
    title: "Astro Starlight project initialized at web/docs/ (TS strict, MDX, Pagefind)"
    status: PASS
    evidence:
      - "pnpm create astro@latest --template starlight succeeded; web/docs/ scaffolded"
      - "package.json has @astrojs/starlight ^0.38.4, astro ^6.1.9, sharp ^0.34.5"
      - "tsconfig.json uses strict mode (Astro template default)"
      - "pnpm.onlyBuiltDependencies: [esbuild, sharp] approved post-install scripts"
      - "Pagefind ships with Starlight; index built at build time (build log: 'Found 6 HTML files. Finished building search index in 52ms')"

  AC2:
    title: "5 docs pages migrated (index/quickstart/api/sdk/errors)"
    status: PASS
    evidence:
      - "web/docs/src/content/docs/{index,quickstart,api,sdk,errors}.mdx all created"
      - "Build output: dist/{index,quickstart,api,sdk,errors}/index.html — 6 pages incl. 404"
      - "MDX uses Starlight components (Card, CardGrid, Tabs, TabItem, Aside, Badge)"
      - "Multi-image i2i added in api.mdx (input_image_b64_2 row), sdk.mdx (image2 example), errors.mdx (field discriminator)"

  AC3:
    title: "Sidebar IA matches spec (3-level hierarchy, no empty placeholders)"
    status: PASS
    evidence:
      - "astro.config.mjs sidebar array: [Welcome, Get started > Quickstart, API Reference, SDK, Errors] — 5 entries, 3 levels (group → page)"
      - "No empty placeholder pages — Authentication and Releases on Welcome card-grid link to existing GitHub resources (README + Releases page) per AC3 directive"

  AC4:
    title: "Brand identity preserved (charcoal+blue+Inter/JetBrains Mono, dark-only V1)"
    status: PASS
    evidence:
      - "theme.css overrides Starlight CSS vars to obsidian #050505 + charcoal #1c1c1c + blue accent #1d7fe5 (matches web/landing/index.html lines 22-34 Tailwind config)"
      - "Fonts: --sl-font set to Inter system fallback chain; --sl-font-mono set to JetBrains Mono — matches landing line 57"
      - "Forced dark-only: both [data-theme='light'] and [data-theme='dark'] resolve to identical values"
      - "Spec correction: spec's hypothesized teal accent + Geist fonts replaced with the verified blue + Inter/JetBrains Mono after reading actual landing config (documented in story Completion Notes Phase 1.5)"

  AC5:
    title: "Multi-image i2i documented in 3 places (API · SDK · Errors)"
    status: PASS
    evidence:
      - "api.mdx: input_image_b64_2 row in i2i request body table; Aside cross-referencing SDK image2; example response shape includes all 5 _2-suffixed metadata fields"
      - "sdk.mdx: edit() with image2 in Tabs example; ValidationError field: 'image' | 'image2' discriminator; forward-compat callout for 0.4.0 client against 0.3.x server"
      - "errors.mdx: dedicated 'field discriminator (multi-image i2i)' section with example response body and SDK handler pattern"
      - "Semantic verification against ground truth:"
      - "  sdk/src/types.ts:19 — image2?: EditImageInput ✓"
      - "  sdk/src/types.ts:39-43 — input_width_2, input_height_2, source_width_2, source_height_2, input_downsampled_2 ✓"
      - "  sdk/src/validate.ts:120-122 — image2 normalization with field: 'image2' ✓"
      - "  serverless/handler.py:252 — TextEncodeQwenImageEditPlus class_type ✓"
      - "  serverless/deploy.sh:14 — image tag :0.2.1 (multi-image deploy) ✓"
      - "  sdk/package.json:3 — version 0.4.0 ✓"

  AC6:
    title: "Pagefind search indexes 5 pages, returns results for {multi-image, image2, polling, rate limit}"
    status: PASS
    evidence:
      - "Pagefind index built at dist/pagefind/ (5 .pf_fragment files + .pf_index)"
      - "grep -ci 'multi-image' on built HTML: 24 hits (api: 5, sdk: 6, errors: 5, quickstart: 4, index: 4)"
      - "grep -ci 'image2' on built HTML: 12 hits (api: 1, sdk: 6, errors: 5) — exactly the 3 expected pages per AC5"
      - "grep -ci 'polling' / 'rate limit': covered in api.mdx (rate-limit headers section) + errors.mdx + quickstart.mdx"
      - "Note: full UI verification (Pagefind search box returning hits in browser) deferred to post-PR Vercel preview"

  AC7:
    title: "Lighthouse score ≥ 90 on each of 5 pages (Performance · A11y · Best Practices · SEO)"
    status: DEFERRED
    deferred_to: "post-PR Vercel preview review by @qa"
    rationale: |
      Browser automation unavailable in this Claude Code main session (no headless Chrome).
      Dev Completion Notes explicitly flag this as a known gap for @qa pre-merge.
      Recommended command at preview time: pnpm dlx lighthouse <preview-url>/{,quickstart,api,sdk,errors}/
      Reports archive: docs/qa/lighthouse-2.9/

  AC8:
    title: "WCAG AA contrast verified, zero violations"
    status: DEFERRED
    deferred_to: "post-PR Vercel preview review by @qa"
    rationale: |
      Same browser dependency as AC7. Static contrast assessment is favorable
      (white #ffffff on obsidian #050505 ≈ 21:1; #4da3ff on obsidian ≈ 7.5:1; both
      well above AA 4.5:1 minimum), but axe-core CLI run against rendered DOM
      is the canonical verification. Defer to preview deploy.

  AC9:
    title: "No broken internal links post-build (linkinator)"
    status: PASS
    evidence:
      - "pnpm dlx linkinator dist/ --recurse --silent --skip 'github.com|deploy-lp-one' → ✓ 16 links scanned, zero broken"
      - "Output saved to docs/qa/2.9-linkinator.txt"

  AC10:
    title: "Vercel cutover validated (preview approval → root swap → production check)"
    status: DEFERRED
    deferred_to: "Phase 4, @devops authority"
    rationale: |
      Cutover is explicitly Phase 4 @devops scope per Story 2.9 Tasks 9-10.
      This dev run delivered Phases 1-3 only. AC10 will be evaluated when @devops
      executes the cutover and the user/UX-Expert approve the preview.

# === CodeRabbit Findings (committed -t against main) ===
coderabbit:
  scan_date: 2026-04-29
  scan_command: "~/.local/bin/coderabbit --prompt-only -t committed --base main"
  total_findings: 6
  by_severity:
    CRITICAL: 0
    HIGH: 0
    MEDIUM: 1
    LOW: 5

  findings:
    - id: CR-1
      file: web/docs/src/content/docs/api.mdx
      lines: "97-122"
      severity: LOW
      type: clarity
      summary: "JSON response example shows _2-suffixed metadata fields without inline conditional disclaimer (the disclaimer follows in prose immediately below at line 121)."
      recommendation: "Optional polish: add inline JSON comment OR split into two examples (single vs multi-image). Non-blocking — prose disclaimer at line 121 mitigates ambiguity."
      action: noted

    - id: CR-2
      file: web/docs/src/content/docs/quickstart.mdx
      lines: "165"
      severity: LOW
      type: clarity
      summary: "'1:1 is rejected — use a non-square crop' lacks rationale for why square is rejected."
      recommendation: "Optional: append brief rationale (model training/processing limitation). Non-blocking — assertion is technically accurate per sdk/src/validate.ts."
      action: noted

    - id: CR-3
      file: web/docs/src/content/docs/sdk.mdx
      lines: "99"
      severity: LOW
      type: clarity
      summary: "'non-square aspect ratio' wording is ambiguous (could read as either 'rejects square' or 'requires non-square')."
      recommendation: "Optional: rephrase to explicit 'square (1:1) is rejected; all other aspect ratios accepted'. Non-blocking."
      action: noted

    - id: CR-4
      file: docs/design/landing-docs-portal-refactor.md
      lines: "99"
      severity: LOW
      type: reproducibility
      summary: "Spec uses 'pnpm create astro@latest' instead of pinned version."
      recommendation: "Effectively pinned via pnpm-lock.yaml (astro@6.1.10 resolved). Spec-doc cosmetic; future spec updates could pin major."
      action: noted

    - id: CR-5
      file: docs/stories/2.9.docs-portal-refactor.story.md
      lines: "287"
      severity: LOW
      type: process
      summary: "Subtask 10.1 leaves archive sequencing ambiguous (in-PR vs post-merge)."
      recommendation: "Defer: Subtask 10.1 is @devops authority (Phase 4); they will resolve sequencing when executing cutover."
      action: noted

    - id: CR-6
      file: docs/stories/2.9.docs-portal-refactor.story.md
      lines: "89-102"
      severity: MEDIUM (reclassified to NOT-A-DEFECT)
      type: documentation
      summary: "Locked decisions table mentions Geist fonts and (per spec) teal accent — both contradict the actual implementation (Inter + JetBrains Mono, blue accent)."
      recommendation: |
        NOT-A-DEFECT per AIOX flow: Dev Notes captures the historical decision
        snapshot at the moment of PO validation; Completion Notes (Dev Agent
        Record) carries the corrected reality. The drift is the intended
        historical record. Per @qa story-file-permissions, QA cannot edit
        Dev Notes; per @dev story-file-updates-ONLY rules, neither can @dev.
        Future remediation: @sm or @po could append an "Implementation note"
        below the locked-decisions table referencing the Phase 1.5 Completion
        Notes — but this is hygiene, not correctness.
      action: documented_as_design_intent

# === Risk Profile ===
risk_profile:
  overall: LOW
  rationale: |
    Story is documentation-only (Astro static site). No application code, no auth changes,
    no DB migrations, no infrastructure modifications, no SDK runtime behavior changes.
    Vercel cutover (Phase 4) is the single elevated-risk operation, but it has a clean
    rollback path (Vercel root setting can revert in one click; old web/landing/ kept
    in web/_archive/) and is @devops-managed.
  risks_addressed:
    - "Multi-image i2i semantic accuracy: VERIFIED against SDK ground truth (types.ts, validate.ts, CHANGELOG.md)"
    - "Brand drift from landing: AVOIDED via Tailwind config extraction (accent corrected from spec's wrong teal to actual blue)"
    - "Broken links: VERIFIED zero by linkinator"
    - "Search indexability: VERIFIED via grep on built HTML"
  residual_risks:
    - "Lighthouse < 90 on some metric: LOW probability (Astro static + Starlight is fast by default); detected at preview"
    - "Contrast violation on obscure component: LOW probability (calculated ratios are well above AA); detected at preview via axe"
    - "Vercel root cutover causes brief service blip: MITIGATED by @devops 1-click rollback (Phase 4 Subtask 9.9)"

# === Recommended actions ===
recommendations:
  for_dev:
    - "[Optional, low-effort] Address CR-1 by adding inline note in api.mdx response JSON to disambiguate conditional _2 fields"
    - "[Optional] Address CR-2 / CR-3 wording polish in quickstart.mdx and sdk.mdx for clarity"
  for_devops:
    - "[Required for AC7 + AC8 + AC10] Execute Phase 4: open PR → deploy preview → request @qa Lighthouse + axe runs → swap Vercel root on approval"
    - "[Phase 4 sequencing decision per CR-5] Decide whether Task 10.1 (web/landing → web/_archive/) lands in main PR or post-merge cleanup"
  for_qa:
    - "[Post-PR] Run Lighthouse against Vercel preview URL for all 5 pages, archive reports under docs/qa/lighthouse-2.9/"
    - "[Post-PR] Run @axe-core/cli or browser axe against preview, document zero violations"
    - "[Post-cutover] Re-gate with verdict update (CONCERNS → PASS) once AC7+AC8+AC10 evidence captured"

# === Gate Decision ===
decision:
  verdict: CONCERNS
  blocking_for_merge: false
  rationale: "All static + semantic checks pass. Browser-mediated ACs (Lighthouse, axe, visual parity) are deferred by design — they require a deployed preview which @devops will produce in Phase 4. CodeRabbit findings are 0 CRITICAL / 0 HIGH; remaining are clarity polish suggestions that don't block production safety."
  next_step: "@devops *push to open PR; deploy preview; @qa re-runs gate against preview for AC7+AC8 closure; user + @ux-design-expert review preview; @devops cuts over."
