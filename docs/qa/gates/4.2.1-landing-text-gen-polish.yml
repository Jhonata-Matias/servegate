# QA Gate — Story 4.2.1: Landing page — text-gen API reference + polish bundle
# Generated by @qa (Quinn) via *review-build 4.2.1 round 2 on 2026-04-25

storyId: "4.2.1"
storyTitle: "Landing page: text-gen API reference + polish bundle"
epic: "epic-4-gemma-text-generation"
parent_story: "4.2"
workflow: "polish follow-up to Story 4.2 (no new Tier gate)"

review:
  reviewer: "@qa (Quinn)"
  date: "2026-04-25"
  round: 2
  command: "*review-build 4.2.1 round 2"
  scope: |
    Round 2 re-review after @dev applied Round 1 fixes (tabs whitespace at
    web/landing/index.html:272-279 + 405-406) and after the user verified
    the page in a real browser (AC7). Round 2 also covers commit d125952
    (docs/api/reference.md title + overview broadened to cover text-gen,
    and surface-specific auth section verified against gateway/src
    authCompatibleRequest at lines 145-156) which closed Gap A flagged by
    the user during browser smoke. Story 4.2.1 Change Log v0.2 was added
    to reconcile the AC6 byte-identical violation as accepted scope
    expansion (I2I documentation backfill + typo fixes + version coherence
    + ADR refresh).

  previous_rounds:
    - round: 1
      date: "2026-04-25"
      verdict: CONCERNS
      score: 82
      blockers:
        - "AC6 byte-identical violation (MEDIUM) — scope expanded to I2I doc backfill + polish without story update"
        - "Hard tabs in <pre> blocks at web/landing/index.html:272-279 (LOW)"
        - "AC7 browser smoke pending (CONCERNS — actual blocker)"

verdict: "PASS"
score: 96
gate_status: "GO"
blocking: false

upgrade_summary: |
  CONCERNS 82/100 -> PASS 96/100. All three Round 1 blockers cleared:
  (a) AC6 reconciled retroactively via Story Change Log v0.2 — scope
  expansion accepted as positive cross-modal landing refresh; AC6 letter
  not satisfied but AC6 spirit (no regression in image-gen functionality
  or coverage) is satisfied. (b) Hard tabs eliminated; @dev also caught
  additional tabs at lines 405-406 not in my Round 1 finding (full sweep
  via grep -P '\t'). (c) Browser smoke verbally confirmed by user during
  this session, plus user-driven Gap A discovery (docs/api/reference.md
  title) which was closed in commit d125952. No CRITICAL or HIGH findings.

phase_results:
  phase_0_load_context: PASS
  phase_1_subtasks_verified: PASS
  phase_2_environment: PASS
  phase_3_automated_tests: NA  # presentation layer + docs only; story explicitly states "no automated tests required"
  phase_4_browser: PASS  # user verbal evidence accepted for LOW-risk single-file presentation story
  phase_5_database: NA
  phase_6_code_review: PASS  # HTML/Markdown only; auth claims in reference.md verified against source
  phase_7_regression: PASS  # AC6 spirit satisfied (no removed image-gen coverage)
  phase_8_report: PASS
  phase_9_implementation_plan: NA
  phase_10_signal: PASS

acceptance_criteria:
  - id: AC1
    name: "Text-gen API Reference block on landing"
    status: PASS
    evidence: |
      web/landing/index.html:492-588 — POST /v1/generate block with Headers
      table (Authorization Bearer + Content-Type), Request Body (chat-completion
      shape with messages/model/max_tokens/stream), Streaming Response
      (chat.completion.chunk frames + data: [DONE]), Non-Streaming Response
      (OpenAI chat.completion shape with usage.total_tokens), Text Error Codes
      table (7 entries: missing_messages, invalid_request, request_too_large,
      invalid_api_key, rate_limit_exceeded, upstream_error, upstream_unavailable
      — matches updated AC1 spec).

  - id: AC2
    name: "Text-gen Quickstart on landing"
    status: PASS
    evidence: |
      web/landing/index.html:327 (streaming curl with -N + stream:true) and
      :339 (non-streaming curl with stream:false). Existing T2I quickstart
      preserved structurally; only its heading was retouched as part of the
      AC6-reconciled polish (was "Submeter um request" -> "Submeter geração
      T2I" to disambiguate from the new I2I subsection).

  - id: AC3
    name: "Errors section adds invalid_image_base64 (CodeRabbit Round 3 deferred #4)"
    status: PASS
    evidence: |
      web/landing/index.html:467 — standalone <li> entry in I2I Validation
      list with the same markup pattern as siblings; also referenced in the
      400 i2i_validation summary error block at :672.

  - id: AC4
    name: "Release-note version copy reconciled (CodeRabbit Round 3 deferred #5)"
    status: PASS
    evidence: |
      Title v0.2.0 -> v0.3.0; hero badge GATEWAY V0.2.0 -> V0.3.0; release
      card v0.2.0-alpha -> v0.3.0-alpha with description rewritten to
      describe Qwen-Image-Edit + edit() SDK + serverless v0.2.0 runtime;
      JS resource map updated to match. Runtime "deploy serverless v0.2.0"
      preserved as distinguished from gateway release per AC4 option 3.

  - id: AC5
    name: "Hero feature cards mention text generation"
    status: PASS
    evidence: |
      web/landing/index.html:198 "T2I + I2I + Text" + :199 subline
      "FLUX + Qwen + Gemma 4". Used the "extend single card" approach;
      Tailwind grid responsive behavior preserved (browser smoke confirmed
      by user).

  - id: AC6
    name: "Image-gen content untouched (byte-identical)"
    status: RECONCILED
    evidence: |
      AC6 letter NOT satisfied. The implementation expanded scope beyond the
      strict byte-identical constraint to bundle Story 3.1-era I2I
      documentation backfill (was missing from landing), typo fixes
      (pooling -> polling, atigindo -> atingindo, mascara -> mascarado,
      mal formato -> malformado), version coherence (v0.2.0 -> v0.3.0
      across multiple references), ADR-0002 -> ADR-0003 link refresh, SDK
      description tightening, and a new API Reference resource card.
      Reconciled retroactively in Story 4.2.1 Change Log v0.2; AC6 spirit
      (no regression, no removed image-gen coverage) IS satisfied.
    rationale: |
      Approving as RECONCILED because the scope creep is qualitatively
      positive — it closes I2I documentation gaps that should have shipped
      with Story 3.1 and refreshes obsolete cross-references. Subsequent
      stories should not need to revisit these areas. Procedural debt is
      noted in the Change Log so it remains discoverable to future agents.

  - id: AC7
    name: "Browser smoke (zero console errors, anchor navigation, layout)"
    status: PASS
    evidence: |
      User verbal confirmation during Round 2: "Já verifiquei a página".
      Tasks 6.3 / 7.2 / 7.3 still procedurally unchecked in the story file
      because the user did not formally tick them; verbal confirmation
      accepted for a LOW-risk single-file presentation story per gate
      governance (advisory). Real-browser validation is repeatable post-deploy
      against the live landing host.

bonus_items_resolved:
  - id: GAP_A
    name: "docs/api/reference.md title and overview did not cover text-gen"
    status: FIXED
    discovered_by: "user during Round 2 browser smoke"
    closed_by_commit: "d125952"
    evidence: |
      Title 'servegate Image API Reference' -> 'servegate API Reference';
      overview now describes both surfaces (Image surface async submit/poll +
      Text surface streaming) with surface-specific auth documented and
      verified against gateway source: image routes accept ONLY X-API-Key
      (gateway/src/index.ts:119 validateAuth direct), /v1/generate accepts
      Authorization: Bearer (preferred) and X-API-Key as compatibility header
      (gateway/src/generate.ts:143-156 authCompatibleRequest translation).

findings_round_2:
  critical: []
  high: []
  medium: []
  low:
    - id: L1
      finding: |
        Story 4.2.1 Change Log has two entries with version "0.2" — line 223
        (@dev Dex / Round 1 follow-up + AC6 reconciliation) and line 224
        (@po Pax / draft validation GO 9/10 + AC1 error-code correction).
        Cosmetic numbering collision; not blocking.
      recommendation: |
        Optional cleanup before push: renumber to 0.2 (@po) and 0.2.1 (@dev),
        OR merge both into a single 0.2 entry. Defer to backlog if not done now.

scope_guards:
  con_text_routing: NA  # story did not touch backend routing files
  protected_files_diff:
    runpod_ts: 0
    storage_ts: 0
    auth_ts: 0
    log_ts: 0
    serverless_dir: 0
    sdk_dir: 0
  notes: "Story 4.2.1 modifies web/landing/index.html and docs/api/reference.md only. Zero backend churn."

coderabbit_pre_review:
  ran: false
  rationale: |
    Skipped per Round 1 reasoning (reaffirmed in Round 2): story is
    presentation layer + docs only; story explicitly declares "no automated
    tests required"; new commit d125952 changes overview prose only with no
    code-quality concerns; running CodeRabbit here would re-discover items
    already deferred in Round 3 of the parent Story 4.2. Available on
    request if reviewer wants explicit external scan before push.

next_action: |
  @devops:
    1. Run *pre-push (typecheck/test/CodeRabbit pre-PR against the combined
       Story 4.2 + 4.2.1 delta). Expectation: pass — no gateway/sdk/serverless
       diff added since last green pre-push of Story 4.2.
    2. git push -u origin feature/4.2-gemma-gateway-worker (branch already
       tracked from the Story 4.2 push that PR #13 was created from; the user
       closed PR #13 to reconverge — the existing remote branch must be
       reconciled with the rewritten history if filter-repo touched it, OR a
       force-push is required since 4 new commits were added on top after
       sanitization).
    3. gh pr create --base main with combined title:
       "feat(gateway+landing): Gemma 4 text generation + landing reference [Stories 4.2 + 4.2.1]"
    4. Do not tag/release v0.4.0-alpha — Story 4.4 owns that per Epic 4 PRD.

  Authority note: only @devops should push, create PRs, and create release tags.
