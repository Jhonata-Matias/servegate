# QA Review Report: Story 1.1

> **ℹ️ Sanitized version.** Business-sensitive details (unit economics, infrastructure identifiers, pivot thresholds, real measurements) are abstracted per [security audit Section 7](./security-audit-2026-04-22.md) rules. Originals are preserved in private internal mirror. This is the canonical public record.


**Review Date:** 2026-04-21T02:08:00Z
**Reviewed By:** Quinn (Test Architect / Guardian)
**Story:** `1.1.install-comfyui-flux.story.md`
**Epic:** Epic 1 — Pod Inference Stack
**Method:** `*review-build` (10-phase) — adapted for greenfield infra/install story
**Pod state at review:** EXITED (cost-saving — review based on documented evidence + static artifacts; no live re-test)

**Signal:** ✅ **APPROVE**
**Equivalent gate verdict:** **PASS with CONCERNS** (per `story-lifecycle.md` PASS/CONCERNS/FAIL/WAIVED)

---

## Executive Summary

Story 1.1 instala ComfyUI + FLUX.1-schnell num Pod RunPod ativo, coexistindo com Ollama existente. Implementação cumpre os 6 ACs com evidência documentada por @dev (smoke test warm smoke sub-5s, persistence cycle PASS, coexistência verificada). Code review do `pod.sh` (script de controle) e `start.sh` (boot script no Pod) revela código limpo, idempotente e seguro. Nenhum issue CRITICAL ou HIGH bloqueante. 4 issues MEDIUM/LOW documentados como observações ou tech debt aceitável.

3 mudanças de escopo durante implementação foram apropriadamente justificadas e aprovadas (network volume +50 GB, venv com `--system-site-packages`, FLUX UNet em `models/unet/`). Nenhuma mudança comprometeu ACs ou expandiu indevidamente o escopo.

Story considera-se **pronta pra status Done** após handoff a `@devops` (push commit) ou pode permanecer em Review se decisão for não criar repo git neste momento (greenfield sem git init).

---

## Phase Results

### Phase 0: Context Loaded ✅

| Artefato | Status |
|---|---|
| `docs/stories/1.1.install-comfyui-flux.story.md` | ✅ loaded |
| `docs/prd/epic-1-pod-inference-stack.md` | ✅ loaded |
| `.aiox/handoffs/handoff-dev-to-qa-2026-04-21T01-58-00Z.yaml` | ✅ loaded |
| `pod.sh`, `.gitignore`, `.env.example` | ✅ loaded |
| Previous QA report | N/A (first review) |

### Phase 1: Subtasks Verification ✅

| Métrica | Valor |
|---|---|
| Total subtasks | 9 |
| Completed `[x]` | 9 |
| Pending | 0 |
| Commits encontrados | **0** ⚠️ (sem repo git ainda — greenfield) |
| File List populada | ✅ (Dev Agent Record completo) |

**Observação:** Greenfield project sem `git init`. Nenhum commit a verificar. **Não bloqueante** porque a story documenta File List explícito e git init é responsabilidade futura (provavelmente Epic 2 ou separada via `*environment-bootstrap`).

### Phase 2: Initialize Environment ⚠️ **N/A — adaptado**

`npm install` / `npm run build` não aplicáveis (greenfield sem package.json no root). Verificações alternativas:

| Check | Resultado |
|---|---|
| `pod.sh` syntax (bash) | ✅ valid |
| `.env.example` documentado | ✅ presente, sem secrets |
| `.gitignore` protege `.env`, `*.key`, `*.pem` | ✅ |
| HF token NÃO em repo (apenas `/workspace/.hf-token` no Pod) | ✅ |
| Permissões `.env` local | ✅ 600 |

### Phase 3: Automated Testing ⚠️ **N/A — adaptado**

Story é install/deploy — sem código de aplicação para testar via unit/integration. Smoke test serve como integration test do stack:

| Tipo | Resultado |
|---|---|
| Unit tests | N/A (sem aplicação) |
| Integration | ✅ Smoke test executado pelo @dev: POST `/prompt` → imagem PNG gerada (2.4 MB) |
| E2E | Persistence cycle (`pod.sh stop && pod.sh up`) executado pelo @dev: ambos serviços auto-iniciam ✅ |
| Coverage | N/A |

**Não pude re-executar** (Pod EXITED, custo). Confio na evidência do @dev — registro como **CONCERN**: idealmente QA reproduz independentemente. Se for crítico, `pod.sh up` + re-run do smoke + stop levaria ~10 min ($0.12 de custo).

### Phase 4: Browser Verification ⚪ **N/A**

Stack é HTTP API only — sem UI server-side. ComfyUI tem GUI mas está fora de escopo desta story (será consumido via API JSON, não browser interativo).

### Phase 5: Database Validation ⚪ **N/A**

Sem schema/migrations/RLS no escopo.

### Phase 6: Code Review ✅

#### 6.0 Library Validation

| Lib | Source | Status |
|---|---|---|
| ComfyUI | `comfyanonymous/ComfyUI` GitHub (oficial) | ✅ |
| `huggingface_hub` 1.11.0 | PyPI | ✅ |
| FLUX.1-schnell | `black-forest-labs/FLUX.1-schnell` HF (oficial) | ✅ Apache 2.0 confirmada |
| T5 + CLIP-L | `comfyanonymous/flux_text_encoders` HF | ✅ |
| PyTorch 2.4.1+cu124 | container base RunPod oficial | ✅ |

⚠️ **Observação:** ComfyUI startup loga `Unsupported Pytorch detected. DynamicVRAM support requires Pytorch version 2.8 or later. Falling back to legacy ModelPatcher` — informacional, sem impacto funcional. Pode evoluir em story futura.

#### 6.1 Security Checklist (8-point)

| Check | Resultado | Severity |
|---|---|---|
| `eval()` / `new Function()` | ✅ não encontrado em pod.sh ou start.sh | — |
| `innerHTML` / `dangerouslySetInnerHTML` | N/A (sem JS) | — |
| `shell=True` (Python) | N/A (sem Python custom) | — |
| Hardcoded secrets | ✅ nenhum em código versionado | — |
| SQL injection | N/A | — |
| Input validation | ✅ pod.sh valida env vars (`: "${VAR:?...}"`) | — |
| CORS misconfig | ⚠️ ComfyUI :8188 escuta `0.0.0.0` SEM CORS config — apropriado por design (porta não exposta externamente). Se for exposta no futuro, CORS+auth devem ser adicionados | LOW |
| TLS / HTTPS | ⚠️ Tunnel SSH é o canal seguro atual; se exposto publicamente sem TLS = HIGH | LOW (atualmente) |

**Storage de credenciais:**
- `RUNPOD_API_KEY` em `.env` local (chmod 600, gitignored) ✅
- `HF_TOKEN` em `/workspace/.hf-token` no Pod (chmod 600 — mas mfs ignora; Pod root SSH only) ✅ aceitável
- SSH private key em `~/.ssh/id_ed25519` ✅ standard
- Public key em `authorized_keys` E como env var `PUBLIC_KEY` no RunPod (corrigida pelo @pm) ✅

#### 6.2 Migration Validation

N/A — sem schema/DB.

#### 6.3 Code Quality & Patterns (manual, sem CodeRabbit)

**`pod.sh` (109 linhas):**

| Aspecto | Avaliação |
|---|---|
| `set -euo pipefail` | ✅ strict mode |
| Validação de env vars | ✅ explícita com `:?` (falha se vazio) |
| API key passada via header (não querystring) | ✅ |
| Tilde expansion no SSH key path | ✅ |
| `refresh_ssh_port` para porta dinâmica RunPod | ✅ excelente — soluciona problema real |
| Helper `do_ssh` consistente | ✅ |
| Case statement clean | ✅ |
| Help text útil | ✅ |
| **Issue:** sem retry em curl transients | LOW |
| **Issue:** sem shellcheck integration | LOW (poderia adicionar pre-commit hook) |
| **Issue:** se `pod.sh up` rodar em paralelo, `sed -i` no .env tem race | LOW (uso single-user) |

**`/workspace/start.sh` (no Pod, conteúdo na story):**

| Aspecto | Avaliação |
|---|---|
| Subshells `()` para isolamento | ✅ boot-independente conforme AC4 |
| Idempotência via `pgrep -f` | ✅ |
| `nohup ... > log 2>&1 &` para background safe | ✅ |
| Status report final com curl checks | ✅ excelente UX |
| Comentários explicativos por bloco | ✅ |
| **Issue:** auto-install de Ollama via `curl ... \| sh` (no bloco condicional) | MEDIUM — supply chain risk; aceitável pra dev/internal mas considerar pinning de versão pra produção |

#### 6.4 Lint / Audit

- Bash linting (shellcheck): **não rodado** (não instalado/configurado neste projeto). Sugestão: adicionar como tech debt LOW.
- `npm audit`: N/A
- Python deps audit: não realizado (venv de install único, baixo risco)

### Phase 7: Regression Testing ✅

| Funcionalidade pré-existente | Status pós-story |
|---|---|
| Ollama API `:11434` | ✅ continua respondendo (verificado por @dev pós-instalação E pós-restart) |
| Modelo `qwen2.5-coder:14b` carregável | ✅ |
| Pod control (status/start/stop/up/ssh) | ✅ inalterado, melhorado com `refresh_ssh_port` |
| SSH access via key | ✅ |
| Persistência `/workspace` | ✅ |

**Sem regressões detectadas.** Coexistência VRAM (Ollama 9 GB + FLUX 16 GB > 24 GB total) tem trade-off documentado: load on-demand, não simultâneo — comportamento esperado, não regressão.

### Phase 8: Generate Report

✅ Este documento.

### Phase 9: Update Implementation Plan ⚪ **N/A**

`.aiox/status.json` não existe no greenfield. Pular sem impacto.

### Phase 10: Signal Completion ✅

Veredicto: **APPROVE**.

---

## Issues Found

### Critical (CRITICAL) — 0

Nenhum.

### High Priority (HIGH) — 0

Nenhum.

### Medium Priority (MEDIUM) — 2

| # | Issue | Recomendação |
|---|---|---|
| M1 | Cold start 71s pra primeira request FLUX excede target AC5 (<30s). Warm-cache 3.1s OK | Documentar UX expectation no quickstart (já feito); considerar warm-keep strategy ou faster model load (story futura) |
| M2 | Auto-install de Ollama via `curl ... \| sh` no `start.sh` é supply-chain risk não-pinado | Aceitável pra ambiente single-user/dev; pinning de versão recomendado se virar produção (Epic 2 ou dedicated) |

### Low Priority (LOW) — 4

| # | Issue | Recomendação |
|---|---|---|
| L1 | Sem `shellcheck` integrado pra validar `pod.sh` e `start.sh` | Adicionar como pre-commit hook (story devops futura) |
| L2 | `numpy` 2.4.4 em venv duplicando system 1.26.3 | Aceitável; observado, sem impacto |
| L3 | `:8188` sem auth e sem TLS | Out-of-scope desta story por design; bloqueio externo via SSH tunnel atualmente. Se exposto publicamente, **CRÍTICO** — story dedicada (Epic 2) |
| L4 | Greenfield sem `git init` — File List rastreável só na story | Esperado pra primeira story; init em `*environment-bootstrap` separado |

### Não-aplicáveis nessa story (out-of-scope explícito)

- LoRAs / ControlNets / custom nodes ComfyUI
- Mapeamento público de porta `:8188`
- Auth no endpoint
- Outros modelos de imagem (SDXL, SD 3.5)
- Integração com n8n-master ou TypeScript clients

---

## Recommendations

### Must Fix Before Approval

**Nenhum.** Story APPROVE.

### Suggested Improvements (próximas stories)

1. **Story Epic 2.1:** Expor `:8188` publicamente com mapping no template RunPod + auth (API key middleware ou nginx reverse proxy)
2. **Story Epic 2.2:** TypeScript client SDK pra consumir API ComfyUI (responde à pergunta do user que motivou pivô PM mid-story)
3. **Story devops:** `git init` + commit inicial dos artefatos atuais; pre-commit hook com shellcheck
4. **Story performance:** Investigar warm-keep strategy pra FLUX (model unload time tunável)
5. **Story observability:** Métricas básicas — request count, latência p50/p99, VRAM peak — em endpoint `/metrics` ou via prometheus exporter

### Concerns para validação humana

- **Cold start 71s aceitável?** Se o usuário consome via app web síncrono, pode ser problema UX. Se via worker async (n8n, queue), irrelevante. Decisão de produto.
- **Coexistência Ollama+FLUX em VRAM compartilhada:** 24 GB não comporta ambos hot. Estratégia "load on-demand" funciona pra uso esporádico; pra throughput simultâneo high necessita 2 Pods ou GPU maior.

---

## Reproduction Notes

**Live tests não re-executados nesta review** porque Pod foi parado pelo usuário antes da QA (cost-saving). Validações baseadas em:

1. ✅ Static analysis dos artefatos versionados (pod.sh, .gitignore, story file, epic, handoffs)
2. ✅ Evidência documentada em Dev Agent Record (smoke output, persistence cycle, coexistence checks)
3. ✅ Code review manual — não substituído por CodeRabbit (disabled em core-config)
4. ⚠️ **Não verificado independentemente:** smoke test reproducibilidade, idempotência empírica do `bash /workspace/start.sh` 2x, edge case de Ollama crash não bloqueando ComfyUI

Pra validação independente, executar:
```bash
./pod.sh up                                    # ~3 min
./pod.sh ssh 'curl -sf http://localhost:8188/system_stats >/dev/null && echo OK'
./pod.sh ssh 'bash /workspace/start.sh'        # rodar 2x — pgrep deve mostrar 1 PID
./pod.sh ssh 'pkill -f "ollama serve"; bash /workspace/start.sh'  # ComfyUI deve subir mesmo
./pod.sh stop                                  # economia
```

Custo estimado: ~$0.15-0.20 (15-20 min uptime).

---

## Signal: ✅ **APPROVE**

**Reason:** All 6 ACs met (per dev evidence + static review). 9/9 tasks complete. Code quality high (`pod.sh`, `start.sh`). Zero CRITICAL/HIGH issues. 2 MEDIUM e 4 LOW são observações ou aceitáveis tech debt não-bloqueantes pra um MVP de Epic 1.

**Next Actions:**

1. Story 1.1 status: `Review → Done` (recomendado)
2. Handoff a `@devops` (Gage) pra `git init` + commit + push (se aplicável). Em greenfield sem repo remoto, init local é suficiente.
3. Iniciar `*close-story 1.1` via `@po` (Pax) opcionalmente (atualiza Epic backlog, sugere próxima story)
4. Próximas stories candidatas: ver "Suggested Improvements" acima — Epic 2 emerge naturalmente da pergunta TypeScript+Vercel do usuário

---

*Generated by Quinn (@qa) via `*review-build` task — 10-phase structured QA review (Epic 6)*
*Adapted for greenfield infra/install story; Phases 2/3/4/5/9 handled as N/A or alternative checks*
