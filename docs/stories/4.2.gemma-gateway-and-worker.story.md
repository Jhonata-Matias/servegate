# Story 4.2 — Gemma Gateway + RunPod vLLM Text Endpoint

**Status:** Ready (validated by @po 2026-04-24, 10-point GO 10/10, IRS 9/10, HIGH confidence)
**Epic:** [Epic 4 — Gemma Text Generation](../prd/epic-4-gemma-text-generation.md)
**Workflow:** `wf-gated-model-serve` — Phase 1 (Protected Gateway) + Phase 2 (Serverless Backend) — Tier 1 BLOCKING
**Gates:** `GATEWAY_LIVE` (GMS_GW_001) + `BACKEND_LIVE` (GMS_BE_001)

```yaml
executor: "@dev"
secondary_executor: "@devops"
quality_gate: "@architect"
secondary_quality_gate: "@qa"
quality_gate_tools: [code_review, pattern_validation, integration_testing, regression_testing, sse_streaming_verification]
epic: epic-4-gemma-text-generation
adr_ref: adr-0004-gemma-text-gen-stack
workflow_phases: [1, 2]
workflow_gates: [GATEWAY_LIVE (GMS_GW_001), BACKEND_LIVE (GMS_BE_001)]
risk_tier: HIGH
effort_budget: 4-5 dev-days
created: 2026-04-24
author: "@sm River"
depends_on: [4.1]
blocks: [4.3]
```

---

## User Story

**As** a servegate alpha consumer (developer, SDK user, curl tester),
**I want** a `POST /v1/generate` endpoint that returns streaming SSE of Gemma 3 4B text completions through the same authenticated, rate-limited gateway that already serves image generation,
**so that** I can use one base URL + one API key for both image and text modalities, with ChatGPT-grade streaming UX and a token-budget rate-limit that fairly reflects text-gen cost.

---

## Context

Story 4.1 closed the Foundation gate (`FOUNDATION_SET` GO 5/5). ADR-0004 is **Accepted**. This story implements the two blocking Tier-1 phases of the `wf-gated-model-serve` blueprint simultaneously, because the HTTP contract (Phase 1.1) is already specified in [`gemma-gateway-decision.md`](../architecture/gemma-gateway-decision.md) §4 and unblocks backend implementation (Phase 2) in parallel.

**Phase 1 scope (Gateway):** add `POST /v1/generate` route to the existing Cloudflare Worker at `gateway/` with native SSE streaming pass-through, token-budget rate-limit, CORS preflight, client-abort → upstream-abort propagation.

**Phase 2 scope (Backend):** provision NEW dedicated RunPod Serverless endpoint `endpoint-text-gen-alpha` running `runpod-workers/worker-vllm:v2.14.0` with `google/gemma-3-4b-it` on L4 24GB flex, HF_TOKEN secret, 50GB network volume pre-warmed.

**Explicit out of scope:**
- SDK changes (Story 4.3)
- Example notebooks / CLI (Story 4.3)
- TERMS/PRIVACY Prohibited Use Policy bullets (Story 4.4)
- Premium tier `gemma-3-27b-it` on A100 (Epic 5 candidate)
- Per-key multi-tier rate limits (Story 4.3+)
- Real-time per-token mid-stream rate-limit enforcement (deferred per ADR-0004)

**Critical first task:** a BLOCKING spike validating `new Response(upstream.body, …)` SSE pass-through against a real RunPod vLLM endpoint must succeed **on Day 1** before any other Phase 1 code is written. If the spike fails, Story 4.2 pauses for re-planning (TransformStream shim option).

**Non-negotiable regression guarantee (CON-text-routing):** image-gen routes (`POST /jobs`, `GET /jobs/{id}`) MUST remain byte-identical. This story does NOT edit `gateway/src/runpod.ts`, `gateway/src/storage.ts`, `handleSubmit`, `handleStatus`, `submitJob`, or `getStatus` in `gateway/src/index.ts`. New code lives in new modules; shared helpers from `auth.ts` + new additive functions in `rate-limit.ts` are the only points of reuse.

---

## Acceptance Criteria

### AC1 — Pre-flight setup completed (Phase 2 runbook)
- [ ] Maintainer accepts Gemma 3 EULA on HuggingFace for `google/gemma-3-4b-it` (manual click on model card)
- [ ] HF read-scoped token minted with access to `google/gemma-3-*` repos; stored in password manager + calendar reminder set for 90-day rotation (2026-07-24)
- [ ] One-shot RunPod pod (cheapest GPU sufficient; pod destroyed after use) pre-warms `/runpod-volume/models/gemma-3-4b-it` via `hf download google/gemma-3-4b-it --token $HF_TOKEN --local-dir /runpod-volume/models/gemma-3-4b-it`
- [ ] Pre-warm evidence captured in `.aiox/notes/story-4.2/prewarm-evidence-$(date).md`: download duration, byte count matches HF model card (~8GB bf16 weights + ~50GB full repo with vision tower), manifest confirmation, pod ID cleaned up

### AC2 — RunPod text endpoint provisioned
- [ ] NEW serverless endpoint `endpoint-text-gen-alpha` exists in RunPod account
- [ ] Endpoint image is `runpod/worker-v1-vllm:v2.14.0` (pinned, NOT `:latest`)
- [ ] GPU preference list: `L4_24GB → RTX_3090_24GB → A5000_24GB → RTX_4090_24GB` (cheapest-first fallback)
- [ ] Env vars set per ADR-0004 Implementation Notes: `MODEL_NAME=google/gemma-3-4b-it`, `HF_TOKEN=${{RUNPOD_SECRET_HF_TOKEN}}`, `MAX_MODEL_LEN=8192`, `GPU_MEMORY_UTILIZATION=0.90`, `MAX_NUM_SEQS=16`, `MAX_CONCURRENCY=4`, `DTYPE=bfloat16`, `RAW_OPENAI_OUTPUT=1`, `BASE_PATH=/runpod-volume`
- [ ] Worker config: `min_idle=0`, `max=3`, `idle_timeout=60s`, `flashboot=true`, `network_volume=50GB @ /runpod-volume`, `execution_timeout=300s`
- [ ] First cold-start succeeds within 180s; response from `/openai/v1/chat/completions` with `{"model":"google/gemma-3-4b-it","messages":[{"role":"user","content":"hi"}],"max_tokens":5}` returns HTTP 200 with valid OpenAI-shape JSON
- [ ] Existing image-gen endpoint (`RUNPOD_ENDPOINT_ID` binding) untouched; second endpoint is fully decoupled

### AC3 — SSE pass-through spike succeeds (BLOCKING; Day 1 FIRST)
- [ ] One-page spike doc `.aiox/notes/story-4.2/spike-sse-passthrough-$(date).md` exists with:
  - Test command: curl against real RunPod endpoint with `"stream":true`
  - Raw upstream frame samples (5 consecutive `data: {...}\n\n` frames) captured
  - Cloudflare Worker test route deployed to a preview env that does `return new Response(upstream.body, { headers: { 'Content-Type': 'text/event-stream' } })`
  - End-to-end curl from laptop → CF preview → RunPod endpoint confirms SSE frames stream in real-time (no buffering)
  - TTFB measured, compared to direct-to-RunPod TTFB (CF overhead should be < 200ms)
- [ ] If spike FAILS: Story 4.2 status returns to Draft, create follow-up spike story for TransformStream shim, DO NOT proceed to AC4-10
- [ ] If spike PASSES: proceed. Spike doc + evidence is the artifact closing R1 from `gemma-gateway-decision.md` §9

### AC4 — Gateway new modules follow isolation rule (CON-text-routing)
- [ ] New file `gateway/src/generate.ts` (~100 LOC) owns `handleGenerate(req, env, ctx)` — the `POST /v1/generate` handler
- [ ] New file `gateway/src/runpod-text.ts` (~60 LOC) owns `forwardToTextEndpoint(body, env, signal)` — thin OpenAI-compatible client to `endpoint-text-gen-alpha` (distinct from `runpod.ts`)
- [ ] New file `gateway/src/sse.ts` (OPTIONAL, only if spike requires; ~40 LOC) owns framing helpers if we need to interpose for `usage` extraction
- [ ] `gateway/src/runpod.ts` — **zero diff** (image-gen submit/poll untouched)
- [ ] `gateway/src/storage.ts` — **zero diff** (image-gen job mapping untouched)
- [ ] `gateway/src/index.ts` — ONLY adds new route branches: `POST /v1/generate` → `handleGenerate`, `OPTIONS *` → new preflight helper; existing `handleSubmit`, `handleStatus` branches unchanged byte-for-byte
- [ ] `git diff main...HEAD -- gateway/src/runpod.ts gateway/src/storage.ts` returns empty

### AC5 — `/v1/generate` contract matches gateway-decision.md §4
- [ ] Request body validated: `messages[]` required with role∈{system,user,assistant} + content; optional `model` (default `google/gemma-3-4b-it`), `max_tokens` (default 512, cap 2048 alpha), `temperature` (0.0-2.0), `top_p` (0.0-1.0), `stream` (default true)
- [ ] Streaming response: upstream `text/event-stream` passed through via `new Response(upstream.body, …)`; headers `Content-Type: text/event-stream`, `Cache-Control: no-cache`, `Connection: keep-alive`
- [ ] Non-streaming response (`stream:false`): `application/json` with OpenAI `chat.completion` shape; `usage.total_tokens` echoed
- [ ] Error codes per table: 400 (invalid_json, invalid_request, missing_messages), 401 (unauthorized), 413 (request_too_large >2MB body), 429 (rate_limit_exceeded), 502 (upstream_error), 503 (upstream_unavailable), 504 (generation_timeout)
- [ ] All responses include headers: `X-RateLimit-Limit: 50000`, `X-RateLimit-Remaining: <n>`, `X-RateLimit-Reset: <iso>`, `X-Gateway-Model: google/gemma-3-4b-it`

### AC6 — Token-budget rate-limit implemented (additive to existing rate-limit.ts)
- [ ] `gateway/src/rate-limit.ts` gains new exports: `tokenDayKey(now)`, `checkTokenBudgetAndReserve(kv, approxTokens, now)`, `recordTokenUsage(kv, tokens, now)`, `readTokenBudget(kv, now)` — parallel namespace to existing `dayKey`/`checkAndIncrement`/`checkAndRead`
- [ ] KV key format: `tokens:YYYY-MM-DD` (parallel to existing `count:YYYY-MM-DD`); TTL 48h
- [ ] Daily budget constant: `TOKEN_DAILY_LIMIT = 50000`
- [ ] Pre-flight check estimates `max_possible_tokens ≈ len(body_json)/4 + max_tokens`; rejects with 429 if current usage + estimate > 50000
- [ ] Post-flight accounting via `ctx.waitUntil(recordTokenUsage(kv, usage.total_tokens, now))` — client response not blocked
- [ ] Fallback: if upstream `usage` missing, record `max_possible_tokens` pessimistically
- [ ] Existing `checkAndIncrement`/`checkAndRead`/`DAILY_LIMIT` left intact — image-gen rate-limit unaffected

### AC7 — CORS preflight + auth reuse
- [ ] Global `OPTIONS *` handler returns 204 with `Access-Control-Allow-Origin: <configured origin from env>`, `Access-Control-Allow-Methods: GET, POST, OPTIONS`, `Access-Control-Allow-Headers: Content-Type, Authorization, X-API-Key`, `Access-Control-Max-Age: 86400`
- [ ] Allowed origin read from new env binding `CORS_ALLOWED_ORIGIN` (deployed value: landing page origin; fallback: explicit domain, NEVER `*` in production)
- [ ] `validateAuth` from `gateway/src/auth.ts` reused verbatim (accepts `Authorization: Bearer <key>` OR `X-API-Key: <key>`); no changes to auth.ts

### AC8 — Client-abort → upstream-abort wiring (R4 from gateway-decision)
- [ ] `handleGenerate` passes `request.signal` to the `fetch(upstream, { signal })` call
- [ ] When client closes connection mid-stream, upstream `AbortController` fires → RunPod stops generating
- [ ] Verified with integration test: client aborts after 2 seconds, upstream request terminates within 1s; endpoint log shows cancellation
- [ ] Rate-limit post-accounting still records the partial `usage` if available, OR records pessimistic `max_possible` if not

### AC9 — Env bindings + wrangler.toml updated
- [ ] `gateway/src/types.ts` Env interface gains: `RUNPOD_TEXT_ENDPOINT_ID: string`, `CORS_ALLOWED_ORIGIN: string`
- [ ] `gateway/wrangler.toml` adds text endpoint ID placeholder + CORS origin var; secrets via `wrangler secret put RUNPOD_TEXT_ENDPOINT_ID` (actual endpoint ID from AC2)
- [ ] `gateway/src/types.ts` gains types: `GenerateRequest`, `GenerateResponse`, `GenerateStreamChunk`, `TokenBudgetState`
- [ ] `gateway/src/types.ts` `LogEventName` gains: `generate_submitted`, `generate_rate_limited`, `generate_completed`, `generate_stream_aborted`, `generate_upstream_error`, `generate_invalid_input`

### AC10 — Test suite: new tests green + existing tests unchanged
- [ ] New `gateway/tests/generate.test.ts`: covers request validation, contract-shape, error codes, CORS preflight, auth pass-through, rate-limit trigger, header emission
- [ ] New `gateway/tests/sse.test.ts`: covers SSE pass-through (using mocked upstream emitting 5+ frames), client-abort propagation, partial-frame tolerance, missing `[DONE]` sentinel graceful close
- [ ] New `gateway/tests/rate-limit-tokens.test.ts`: covers `checkTokenBudgetAndReserve` under budget, at budget, over budget, post-flight accounting, KV TTL behavior
- [ ] Existing `gateway/tests/auth.test.ts`, `rate-limit.test.ts`, `runpod.test.ts`, `storage.test.ts`, `integration.test.ts`, `nfr-5-kv-reads.test.ts` — **all pass unchanged** (zero modifications; run full suite to prove)
- [ ] Vitest coverage on new modules ≥ 85% line coverage (matches project baseline)

### AC11 — Production deploy + E2E smoke tests pass
- [ ] `wrangler deploy` succeeds against production gateway Worker with new route + bindings
- [ ] E2E smoke-streaming: `curl -N -H "X-API-Key: $KEY" -H "Content-Type: application/json" https://gateway.example/v1/generate -d '{"messages":[{"role":"user","content":"hi"}],"stream":true}'` returns SSE frames in real-time; full completion within 30s warm
- [ ] E2E smoke-non-streaming: same curl with `"stream":false` returns single JSON with `choices[0].message.content` non-empty + `usage.total_tokens` > 0
- [ ] E2E smoke-rate-limit: sequential 101st call of day returns 429 with `X-RateLimit-Remaining: 0` (simulate by pre-setting `tokens:YYYY-MM-DD` in KV to 49999 + making one request with `max_tokens=2000`)
- [ ] E2E smoke-cold-start: first request after > 10min idle captures TTFB; `X-Runpod-Worker-Id` header present; observed TTFB within p99 180s budget (AC from ADR-0004 FS3 — upgrades FS3 from 9/10 partial to 10/10 on this measurement)
- [ ] E2E smoke-client-abort: long-running request aborted from client side after 2s; upstream cancellation verified in RunPod logs; budget accounts appropriately
- [ ] Image-gen regression smoke: `POST /jobs` (T2I), `POST /jobs` with image (i2i), `GET /jobs/{id}` polling — all return identical shapes/latencies to Story 3.1 baseline (run the Story 3.1 smoke script unchanged)

### AC12 — Documentation updates
- [ ] New `docs/usage/text-gen-deploy.md` runbook: pre-flight (HF EULA + token), endpoint provisioning, volume pre-warm, smoke tests, rollback plan
- [ ] New `docs/usage/text-gen-api-quickstart.md`: 1-page curl example + error table + headers reference (SDK-free usage for alpha external testers)
- [ ] `docs/api/reference.md` updated with `/v1/generate` section (request schema, response shapes, error codes) — additive, image-gen sections untouched
- [ ] Production endpoint URL + text endpoint ID recorded in devops handoff note (NOT in repo — `.aiox/notes/story-4.2/deploy-record-$(date).md`)

### AC13 — Dual QA gates pass
- [ ] `GATEWAY_LIVE` (GMS_GW_001) — Phase 1 checkpoint: smoke test suite passes, 16-item security checklist passes (auth before provider call, invalid keys → 401 no-info-leak, rate-limit atomic, provider token never in response, CORS scoped, etc.)
- [ ] `BACKEND_LIVE` (GMS_BE_001) — Phase 2 checkpoint: handler responds to contract, E2E smoke (cold/warm/concurrent) passes, gateway→backend integration works, provider URL isolated
- [ ] QA gate file `docs/qa/gates/4.2-gemma-gateway-and-worker.yml` created with dual-gate verdict

---

## Tasks / Subtasks

### Task 1 — Pre-flight setup (AC1) — @devops (~3h)
- [ ] Subtask 1.1 — Accept Gemma 3 EULA on HuggingFace for `google/gemma-3-4b-it`
- [ ] Subtask 1.2 — Mint HF read-scoped token; store in password manager; set 90-day rotation reminder
- [ ] Subtask 1.3 — Spin up one-shot RunPod pod on cheapest GPU; attach 50GB network volume; run `pip install 'huggingface_hub[cli]' && hf download google/gemma-3-4b-it --token $HF_TOKEN --local-dir /runpod-volume/models/gemma-3-4b-it`
- [ ] Subtask 1.4 — Verify ~50GB download completes; capture manifest; destroy pod
- [ ] Subtask 1.5 — Evidence doc at `.aiox/notes/story-4.2/prewarm-evidence-<date>.md`

### Task 2 — Provision text endpoint (AC2) — @devops (~2h)
- [ ] Subtask 2.1 — Create endpoint `endpoint-text-gen-alpha` in RunPod Console per AC2 spec
- [ ] Subtask 2.2 — Set HF_TOKEN secret as RunPod Endpoint env var (or RunPod Secret if available)
- [ ] Subtask 2.3 — Deploy + wait for first cold-start; verify `/openai/v1/chat/completions` returns 200 on smoke payload
- [ ] Subtask 2.4 — Record endpoint ID in `.aiox/notes/story-4.2/deploy-record-<date>.md` (NOT repo)

### Task 3 — SSE spike (AC3) — @dev (~4h, BLOCKING)
- [ ] Subtask 3.1 — Raw curl against text endpoint with `stream:true`; capture frames
- [ ] Subtask 3.2 — Write minimal CF Worker test route in a preview deployment
- [ ] Subtask 3.3 — End-to-end streaming test: laptop curl → CF preview → RunPod
- [ ] Subtask 3.4 — Spike doc at `.aiox/notes/story-4.2/spike-sse-passthrough-<date>.md`
- [ ] Subtask 3.5 — GATE: if spike fails, HALT story + create TransformStream follow-up; if passes, tear down preview and proceed

### Task 4 — Gateway modules (AC4 + AC5) — @dev (~6h)
- [ ] Subtask 4.1 — Create `gateway/src/runpod-text.ts` with `forwardToTextEndpoint`
- [ ] Subtask 4.2 — Create `gateway/src/generate.ts` with `handleGenerate`
- [ ] Subtask 4.3 — (Optional) Create `gateway/src/sse.ts` if spike evidence requires framing helpers
- [ ] Subtask 4.4 — Add `POST /v1/generate` route branch in `gateway/src/index.ts` (append, do NOT refactor existing branches)
- [ ] Subtask 4.5 — Request validation + error mapping per contract
- [ ] Subtask 4.6 — Verify zero diff on `runpod.ts` and `storage.ts` via `git diff`

### Task 5 — Token-budget rate-limit (AC6) — @dev (~3h)
- [ ] Subtask 5.1 — Add `tokenDayKey`, `TOKEN_DAILY_LIMIT`, `checkTokenBudgetAndReserve`, `recordTokenUsage`, `readTokenBudget` to `gateway/src/rate-limit.ts`
- [ ] Subtask 5.2 — Wire pre-flight in `handleGenerate`; return 429 with proper headers on breach
- [ ] Subtask 5.3 — Wire post-flight via `ctx.waitUntil` for streaming + sync path
- [ ] Subtask 5.4 — Verify `checkAndIncrement`/`DAILY_LIMIT` image-gen paths untouched

### Task 6 — CORS + types + env (AC7 + AC9) — @dev (~2h)
- [ ] Subtask 6.1 — Add OPTIONS preflight handler in `gateway/src/index.ts`
- [ ] Subtask 6.2 — Add env binding `RUNPOD_TEXT_ENDPOINT_ID` + `CORS_ALLOWED_ORIGIN` to `types.ts`
- [ ] Subtask 6.3 — Add `GenerateRequest`, `GenerateResponse`, `GenerateStreamChunk`, `TokenBudgetState` types
- [ ] Subtask 6.4 — Extend `LogEventName` with 5 generate_* events
- [ ] Subtask 6.5 — Update `wrangler.toml`; doc `wrangler secret put` commands

### Task 7 — Client-abort wiring (AC8) — @dev (~2h)
- [ ] Subtask 7.1 — Thread `request.signal` through `handleGenerate` → `forwardToTextEndpoint`
- [ ] Subtask 7.2 — Write integration test simulating client abort after 2s
- [ ] Subtask 7.3 — Confirm upstream cancellation in RunPod logs during smoke

### Task 8 — Tests (AC10) — @qa + @dev (~6h)
- [ ] Subtask 8.1 — `gateway/tests/generate.test.ts`
- [ ] Subtask 8.2 — `gateway/tests/sse.test.ts`
- [ ] Subtask 8.3 — `gateway/tests/rate-limit-tokens.test.ts`
- [ ] Subtask 8.4 — Run full existing suite (`pnpm test` in `gateway/`) — confirm zero changes in output
- [ ] Subtask 8.5 — Coverage report ≥ 85% on new modules

### Task 9 — Deploy + E2E smoke (AC11) — @devops + @qa (~3h)
- [ ] Subtask 9.1 — `wrangler deploy` to production
- [ ] Subtask 9.2 — Run 6 smoke scenarios; capture evidence
- [ ] Subtask 9.3 — Image-gen regression smoke (Story 3.1 script unchanged)
- [ ] Subtask 9.4 — Record TTFB + cold-start p99 metrics → upgrades ADR-0004 FS3 9/10 → 10/10

### Task 10 — Docs (AC12) — @dev (~2h)
- [ ] Subtask 10.1 — `docs/usage/text-gen-deploy.md` (devops runbook)
- [ ] Subtask 10.2 — `docs/usage/text-gen-api-quickstart.md` (external tester runbook)
- [ ] Subtask 10.3 — Append `/v1/generate` section to `docs/api/reference.md`

### Task 11 — Dual QA gate (AC13) — @qa + @architect (~3h)
- [ ] Subtask 11.1 — 16-item security checklist (auth, rate-limit, forwarding, observability) evaluation
- [ ] Subtask 11.2 — Phase 2 checkpoint verification (handler contract, E2E, isolation)
- [ ] Subtask 11.3 — Create `docs/qa/gates/4.2-gemma-gateway-and-worker.yml` with dual-gate verdict
- [ ] Subtask 11.4 — Transition Story 4.2 Status → Done; unblock Story 4.3

---

## File List (expected diff)

```
# New gateway code
gateway/src/generate.ts                            NEW
gateway/src/runpod-text.ts                         NEW
gateway/src/sse.ts                                 NEW (conditional on spike)

# Gateway modifications (additive only)
gateway/src/rate-limit.ts                          MODIFY (add token functions)
gateway/src/types.ts                               MODIFY (add Env fields, types, log events)
gateway/src/index.ts                               MODIFY (add routes; existing branches untouched)
gateway/wrangler.toml                              MODIFY (add bindings)

# Gateway tests
gateway/tests/generate.test.ts                     NEW
gateway/tests/sse.test.ts                          NEW
gateway/tests/rate-limit-tokens.test.ts            NEW

# Documentation
docs/usage/text-gen-deploy.md                      NEW
docs/usage/text-gen-api-quickstart.md              NEW
docs/api/reference.md                              MODIFY (append section, no diff above)

# QA gate artifact
docs/qa/gates/4.2-gemma-gateway-and-worker.yml     NEW

# Explicitly UNCHANGED (CON-text-routing)
gateway/src/runpod.ts                              ZERO DIFF
gateway/src/storage.ts                             ZERO DIFF
gateway/src/auth.ts                                ZERO DIFF
gateway/src/log.ts                                 ZERO DIFF (unless LogEventName added via types.ts)
serverless/                                        ZERO DIFF (entire tree)
sdk/                                               ZERO DIFF (entire tree)
```

---

## Dev Notes

### Isolation rule (CON-text-routing) — non-negotiable

This story MUST NOT modify these existing paths:
- `gateway/src/runpod.ts` (image-gen submit/poll)
- `gateway/src/storage.ts` (image-gen job mapping)
- `handleSubmit`, `handleStatus`, `submitJob`, `getStatus` function bodies in `gateway/src/index.ts`
- Existing functions in `rate-limit.ts` (`checkAndIncrement`, `checkAndRead`, `DAILY_LIMIT`)
- `serverless/handler.py` (image-gen)
- Existing test files (`runpod.test.ts`, `storage.test.ts`, `rate-limit.test.ts`, `integration.test.ts`, `nfr-5-kv-reads.test.ts`, `auth.test.ts`)

Additive modifications ARE allowed on:
- `rate-limit.ts` (new exports parallel to existing)
- `types.ts` (new interfaces + Env fields + LogEventName cases)
- `index.ts` (new route branches appended; existing branches byte-identical)
- `wrangler.toml` (new bindings appended)

CodeRabbit pre-PR review MUST scope-check the diff and flag any regression-risk edits.

### Why dual-gate in one story

The blueprint specifies Phase 1 and Phase 2 as separate gates (`GATEWAY_LIVE`, `BACKEND_LIVE`) and allows Phase 2 to run in parallel with Phase 1 after 1.1 contract freeze. In this story, contract is already frozen in `gemma-gateway-decision.md` — so Phase 1 implementation and Phase 2 endpoint provisioning can genuinely run concurrently. The dual-gate structure keeps workflow accounting honest without slicing the work into two stories artificially.

### Day 1 spike rule (R1 from gateway-decision §9)

The single biggest risk in Story 4.2 is SSE pass-through failing in the CF Workers runtime under load. The spike (Task 3) MUST be the first implementation activity. If it fails, Story 4.2 halts and a separate story for TransformStream shim (likely +100 LOC and +1 dev-day) is created. Do NOT proceed to Tasks 4-7 if spike fails.

### Token-budget accounting trade-off

Per `gemma-gateway-decision.md` §5 + ADR-0004: we accept KV eventual-consistency tradeoff (same tradeoff as Epic 2 R7 rate-limit). Expected overshoot under concurrent race ≤ 10%. Monitor weekly; tighten budget to 25k if overshoot > 10% sustained.

### Test strategy

- **Unit:** Vitest on new modules (`generate.ts`, `runpod-text.ts`, `sse.ts`, extended `rate-limit.ts`)
- **Integration:** Mock upstream `fetch` to simulate RunPod SSE responses (including edge cases: missing `[DONE]`, connection-drop mid-stream, JSON-corrupted frame)
- **Contract:** Verify response shapes match `gemma-gateway-decision.md` §4 schemas
- **Regression:** Existing test suite unchanged; rerun to prove zero side effects
- **E2E smoke:** Live curl against production endpoint after deploy (runbook in `docs/usage/text-gen-deploy.md`)

### Handoff to Story 4.3

On successful close of Story 4.2:
- Text endpoint is live, `/v1/generate` responds with SSE
- Story 4.3 can transition Draft → Ready
- Story 4.3 owns SDK v0.4.0 additions (`complete()` + `completeStream()`), examples (Colab, CLI, curl), error taxonomy, alpha-access opt-in field
- Naming decision deferred: SDK surface for text (we won't rename existing `generate()` image method — per ADR-0004 Implementation Notes, use `complete()` for text OR namespace as `client.text.complete()`. Final TBD in Story 4.3 by @dev + @ux-design-expert.)

### Predictive quality planning (CodeRabbit Integration)

Per @sm core principle: populate CodeRabbit hints for self-healing pre-commit.

```yaml
coderabbit:
  story_id: 4.2
  risk_tier: HIGH
  specialized_agents_invoked:
    - cloudflare-workers-expert   # SSE + Workers runtime idioms
    - typescript-quality          # Strict types for new modules
    - security-reviewer           # Secret leak checks (never echo HF_TOKEN/RUNPOD_API_KEY in responses/logs)
    - regression-detector         # Scope guard on runpod.ts / storage.ts / handleSubmit / handleStatus
  severity_filter: [CRITICAL, HIGH]  # dev-phase light mode per coderabbit-integration.md
  max_iterations: 2                  # per AIOX self-healing config
  scope_guard:
    forbidden_paths:
      - gateway/src/runpod.ts
      - gateway/src/storage.ts
      - serverless/**
      - sdk/**
    forbidden_functions:
      - handleSubmit
      - handleStatus
      - submitJob
      - getStatus
      - checkAndIncrement
      - checkAndRead
  expected_findings_allowlist:
    - SSE streaming pattern may trigger "potential unclosed stream" false-positive — acknowledge in PR description
    - Token-budget race with KV eventual consistency — documented tradeoff
```

### Branch strategy

@sm creates local feature branch at story kickoff: `git checkout -b feature/4.2-gemma-gateway-worker`

@dev commits locally during Tasks 1-10. Upon AC13 close, @sm notifies @devops for:
- `git push -u origin feature/4.2-gemma-gateway-worker`
- `gh pr create` with body from this story's summary

Merge to main happens only after:
1. CodeRabbit pre-PR review CLEAN or acknowledged-debts
2. @architect code review PASS
3. @qa dual-gate PASS
4. CI green (existing image-gen tests + new text-gen tests)

---

## Risks & Mitigations (propagated from Phase 0 research)

| # | Risk | Severity | Source | Mitigation |
|---|---|---|---|---|
| R1 | SSE pass-through fails in CF Workers runtime | **HIGH** | gateway-decision.md §9 | Day 1 spike (Task 3) — HALT if fails |
| R2 | Cold-start p99 > 180s | MEDIUM | provider-assessment.md §3 | Measure in AC11; if breaches, enable 1 always-on L4 ($11/mo) or bake image — trigger defined |
| R3 | HF_TOKEN revoked / EULA unaccepted | MEDIUM | provider-assessment.md §4 | AC1 enforces token + EULA before endpoint provisioning; pre-warm validates token |
| R4 | Client-abort → upstream keeps generating (burned tokens) | MEDIUM | gateway-decision.md §9 | AC8 wires `request.signal` → upstream AbortController |
| R5 | `RAW_OPENAI_OUTPUT=1` framing edge cases | LOW | provider-assessment.md §7 | `sse.ts` (if needed) tolerates partial frames + treats missing `[DONE]` as stream-end |
| R6 | Token-count race → budget overshoot | MEDIUM | gateway-decision.md §9 | Accept ≤10% overshoot alpha; monitor weekly; tighten to 25k if breached |
| R7 | Scope creep edits to `runpod.ts` | MEDIUM | isolation rule | CodeRabbit scope_guard forbidden_paths + @architect PR review |
| R8 | vLLM OOM on L4 under concurrent long-context requests | MEDIUM | provider-assessment.md §7 | AC2 caps `MAX_MODEL_LEN=8192`; monitor per-request latency |

---

## QA Gate — Dual gate `GATEWAY_LIVE` + `BACKEND_LIVE`

### `GATEWAY_LIVE` (GMS_GW_001) — 5 criteria
| # | Criterion | Evidence source |
|---|---|---|
| 1 | Smoke test suite passes against deployed URL | AC11 E2E scenarios |
| 2 | 16-item security checklist passes | Task 11.1 |
| 3 | Rate-limit blocks at configured threshold | AC11 smoke-rate-limit |
| 4 | Cold-start retry succeeds for 130s+ first-call scenario | AC11 smoke-cold-start |
| 5 | Provider token absent from any response body/header | Task 11.1 security checklist |

### `BACKEND_LIVE` (GMS_BE_001) — 5 criteria
| # | Criterion | Evidence source |
|---|---|---|
| 1 | Handler responds to contract from Phase 1.1 | AC5 + AC11 smoke tests |
| 2 | (N/A for text — warmup endpoint not exposed by worker-vllm; cold-start retry covers it) | AC11 smoke-cold-start |
| 3 | E2E smoke tests pass (cold, warm, concurrent) | AC11 + Task 9 |
| 4 | Gateway → backend integration works end-to-end | AC11 smoke-streaming |
| 5 | Provider URL NOT accessible without auth | AC2 + Task 11.1 isolation check (RUNPOD_API_KEY never in Worker responses) |

### Veto conditions
- `provider_url_accessible_without_auth` → VETO
- `any_security_checklist_item_fails` → VETO
- `rate_limit_leaks_across_keys` → N/A (single-key alpha)
- `cold_start_p95 > 3 * budget (540s)` → VETO, return to ADR revisit

### Decision matrix
- GO: Both gates 5/5 pass + no vetoes
- CONDITIONAL: 9/10 combined + documented mitigation
- NO_GO: <9 → return to relevant task

---

## References

- **Predecessor:** [`4.1.gemma-foundation.story.md`](./4.1.gemma-foundation.story.md) (Done, gate PASS 92/100)
- **Epic:** [`epic-4-gemma-text-generation.md`](../prd/epic-4-gemma-text-generation.md)
- **ADR (Accepted):** [`adr-0004-gemma-text-gen-stack.md`](../architecture/adr-0004-gemma-text-gen-stack.md)
- **Contract source:** [`gemma-gateway-decision.md`](../architecture/gemma-gateway-decision.md) §4, §5, §9
- **Provider config:** [`gemma-provider-assessment.md`](../architecture/gemma-provider-assessment.md) §1, §4, §6
- **Workflow blueprint:** [`wf-gated-model-serve.yaml`](../../squads/squad-creator-pro/workflows/wf-gated-model-serve.yaml) Phase 1 + Phase 2
- **Precedent patterns:** [`3.1.qwen-image-edit-i2i.story.md`](./3.1.qwen-image-edit-i2i.story.md) (test suite structure); [`2.5.gateway-rate-limit-cloudflare.story.md`](./2.5.gateway-rate-limit-cloudflare.story.md) (rate-limit precedent)
- **External:** [runpod-workers/worker-vllm v2.14.0](https://github.com/runpod-workers/worker-vllm/releases/tag/v2.14.0), [Cloudflare Workers Streams](https://developers.cloudflare.com/workers/runtime-apis/streams/)

---

## Change Log

| Version | Date | Author | Notes |
|---|---|---|---|
| 0.1 | 2026-04-24 | @sm River | Initial draft post Story 4.1 close. Phase 1+2 dual-gate scope; 13 ACs; 11 tasks; CON-text-routing isolation; SSE spike Day 1 BLOCKING. story-draft-checklist PASS (READY 9/10). |
| 0.2 | 2026-04-24 | @po Pax | **Status: Draft → Ready.** validate-next-story PASS — 10-point 10/10, Implementation Readiness 9/10, HIGH confidence, zero anti-hallucination findings, executor assignment valid (@dev primary != @architect QA gate). 1 should-fix logged: formalize `coderabbit_integration: { enabled: true }` in core-config.yaml (non-blocking, devops backlog). Unblocks @sm to create local branch + @dev to begin Task 1. |

---

## Dev Agent Record

### Agent Model Used

GPT-5 Codex

### Debug Log References

- 2026-04-24 19:44 -03 — @dev (GPT-5 Codex) `*develop 4.2` started. BLOCKED at Task 3 (SSE spike requires live endpoint). Tasks 1-2 are @devops authority. Zero code changes made (correct boundary respect).
- 2026-04-24 20:30 -03 — @devops (Gage) takeover for Tasks 1-2. Discovery iteration:
  - Volume `<NETWORK_VOLUME_ID>` (ollama-models, US-IL-1, 100GB) approved for REUSE per IDS principle.
  - Created template `<RUNPOD_TEXT_TEMPLATE_ID>` + endpoint `<RUNPOD_TEXT_ENDPOINT_ID>` initially with `runpod/worker-v1-vllm:v2.14.0` + `gemma-3-4b-it`.
  - 2 accidental probe pods created during availability scan (<minor debug burn>, deleted).
  - HF_TOKEN exposed via RunPod GET /pods/{id} — required token rotation by user.
- 2026-04-24 21:30 -03 — vLLM stack failure documented:
  - v2.14.0 → CUDA 12.5+ requirement vs RunPod 4090 hosts CUDA <12.5 (dashboard log: "nvidia-container-cli: unsatisfied condition: cuda>=12.5").
  - v2.13.1 → same CUDA 12.9 base, silent vLLM engine init crash on Gemma 3 multimodal (dashboard log: "engine core initialization failed", "AsyncEngineDeadError").
  - Pivot to gemma-3-1b-it (text-only) → workers report `ready` but jobs stuck IN_QUEUE 19+ min (silent failure beyond container).
  - ~<significant debug burn> in worker-hours across debug cycles.
- 2026-04-24 22:15 -03 — User course-correction: pointed to https://ollama.com/library/gemma4 — **Gemma 4 is the actual target model** (Phase 0.1 missed Gemma 4, used Gemma 3 because Phase 0.1 training cutoff). Volume name `ollama-models` confirms Ollama was always intended runtime.
- 2026-04-24 22:30 -03 — Pivot executed: template image → `svenbrnn/runpod-ollama:0.21.2`, env → `OLLAMA_MODEL_NAME=gemma4:e4b`. Single-worker test:
  - Cold-boot: **<cold-boot duration>** (under 180s budget) — image pull + ollama serve + gemma4:e4b model pull + first inference.
  - Warm path 1: 1.3s wallclock, 557ms exec, delayTime 18ms.
  - Warm path 2: 1.8s wallclock, 1.1s exec, 80 tokens out (~70 tok/s decode).
  - Output sample: `{"role":"assistant","content":"OK"}` for "Say OK" prompt. ✓
- 2026-04-24 22:35 -03 — Image-gen endpoint regression verified intact (3 ready workers, 368 jobs done, 4 historical fails — pre-existing baseline). Zero impact from text endpoint provisioning.

### Completion Notes List

- **Tasks 1-2 (@devops): COMPLETE** with stack pivot from vLLM to Ollama.
- **Task 3 (@dev SSE spike): UNBLOCKED** — endpoint `<RUNPOD_TEXT_ENDPOINT_ID>` live and serving `gemma4:e4b`.
- **ADR-0004 needs amendment** (Gemma 3 → Gemma 4, vLLM → Ollama) — see deploy-record for full delta.
- **SDK Story 4.3 must handle Gemma 4 dual output:** Ollama returns both `message.content` and `message.reasoning` (chain-of-thought built into Gemma 4). With low `max_tokens`, content may be empty.
- **Tasks 4-11 (@dev) ready to start** — gateway code can begin against live endpoint. SSE spike (Task 3) BLOCKING for further work.
- **Cost reality:** <significant debug burn> + <validation cost>. Forward run rate: <forward run rate> at 100 calls/day on RTX 4090 SECURE (within <alpha cost ceiling>).

### File List

- `docs/stories/4.2.gemma-gateway-and-worker.story.md` — Dev Agent Record updated with full Tasks 1-2 evidence (no AC checkboxes — those are for code phase).
- `.aiox/notes/story-4.2/deploy-record-2026-04-24.md` — gitignored deploy record with endpoint ID, full env vars, smoke evidence (NOT in repo).
- `.gitignore` — added `.aiox/notes/story-4.2/deploy-record-*.md` pattern.
- `docs/architecture/adr-0004-gemma-text-gen-stack.md` — to be amended with v1.2 noting pivot (next sub-task).

### Env vars to add to gateway/.env (handoff to @dev for Task 4-7)

```
RUNPOD_TEXT_ENDPOINT_ID=<RUNPOD_TEXT_ENDPOINT_ID>
```

(`RUNPOD_API_KEY` already present.)
