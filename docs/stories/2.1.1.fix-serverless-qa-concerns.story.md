# Story 2.1.1: Fix Serverless Endpoint QA Concerns — deploy reproducibility + input validation + robustness

## Status

Ready

## Executor Assignment

```yaml
executor: "@dev"
quality_gate: "@qa"
quality_gate_tools: [bash, curl, pytest, runpod_api]
```

> Rationale: correções pontuais em código Python + shell + config já existentes. Sem novas decisões arquiteturais (essas estão em Story 2.1.2). Pure dev execution; QA valida via regressão do bench warm + novos unit tests.

## Story

**As a** consumer downstream do Epic 2 (Story 2.2 SDK, Story 2.3 demo web),
**I want** os 6 issues de qualidade flagged pelo QA na Story 2.1 resolvidos (1 Medium + 5 Low),
**so that** o endpoint Serverless seja reprodutível via `deploy.sh` idempotente, a validação de input rejeite valores malformados sem coerção silenciosa, o handler seja resiliente a estados inesperados do ComfyUI, e regressões futuras fiquem cobertas por unit tests.

## Acceptance Criteria

1. **Deploy reprodutibilidade (M2):** `serverless/deploy.sh` aplica `executionTimeoutMs: 300000` (ou valor via env `EXECUTION_TIMEOUT_MS`) em template NOVO; para endpoint existente, PATCH via REST atualiza o valor se divergente — idempotente. Re-rodar deploy.sh duas vezes não altera config final.

2. **Input validation estrita (L1):** `normalize_input` em `serverless/handler.py` rejeita `steps=0` explicitamente (atual aceita silenciosamente como default 4 pelo operador `or`); mesma correção para `width=0` e `height=0`. Comportamento: `None` → default; `0` → `ValueError`. Cobertura via unit test (AC6).

3. **Exception scoping (L2):** `wait_for_comfy` em `serverless/handler.py` captura apenas `(urllib.error.URLError, ConnectionError, OSError, TimeoutError)` em vez de `Exception` amplo. `ValueError`, `ImportError`, e outros bugs estruturais propagam corretamente.

4. **Timeout alignment (L3):** `COMFY_GENERATION_TIMEOUT_S` default em `handler.py` muda de 120 para 280 (margem de 20s abaixo de `executionTimeoutMs=300000`). Também documentado via comment.

5. **Poll robustness (L4):** `poll_history` em `serverless/handler.py` emite log `warn` para `status_str` desconhecidos e continua polling ao invés de potencialmente stall; terminal states continuam sendo apenas `completed=True` ou `status_str=="error"`.

6. **Unit test suite (L5):** `serverless/tests/test_normalize_input.py` com pytest cobre pelo menos estes casos:
   - prompt vazio → ValueError
   - prompt None → ValueError
   - prompt type errado (int, dict) → TypeError ou ValueError
   - steps=0 → ValueError (novo)
   - steps=51 → ValueError
   - steps omitido → default 4
   - width=100 (< MIN_DIM) → ValueError
   - width=1025 (not multiple of 8) → ValueError
   - width=0 → ValueError (novo)
   - seed negativo → ValueError
   - seed omitido → valor aleatório em [0, 2^31)
   - valid defaults → returns sanitized dict com todas as chaves
   - valid full (todos params explícitos) → returns exatamente os inputs

7. **Regressão warm (no perf hit):** Após mudanças, rodar `python3 serverless/tests/latency_bench.py --warm 20 --cold 0` e confirmar warm p95 ≤ 8.5s (margem de 20% sobre baseline 7.0s registrado em 2.1). Resultado documentado no Dev Agent Record.

## 🤖 CodeRabbit Integration

> **CodeRabbit Integration**: Disabled
>
> CodeRabbit CLI is not enabled in `core-config.yaml` (chave `coderabbit_integration.enabled` ausente).
> Quality validation will use manual review process only.
> To enable, set `coderabbit_integration.enabled: true` in `core-config.yaml`.

### Story Type Analysis (manual)

- **Primary Type:** Bug Fix (regression from 2.1 QA)
- **Secondary Type:** Test Coverage (L5 adds unit tests)
- **Complexity:** **SIMPLE** (Spec Pipeline score ~5: mudanças isoladas em 2 arquivos, superfície pequena, determinístico)

### Specialized Agent Assignment (manual)

**Primary:**
- @dev: implementação dos 7 ACs
- @qa: re-review com foco em (a) regressão warm não quebrada, (b) unit tests cobrem os edge cases originais, (c) idempotência do deploy.sh validada por double-execution

### Quality Gate Tasks (manual)

- [ ] Pre-Commit (@dev): `python -m py_compile handler.py`; `pytest serverless/tests/test_normalize_input.py -v`
- [ ] Pre-Review (@dev): `bash -n serverless/deploy.sh` + rodar deploy.sh 2x consecutivas sem diff final no `.env`
- [ ] Pre-Merge (@qa): bench warm p95 ≤ 8.5s documentado

### CodeRabbit Focus Areas (manual)

**Primary:**
- Truthy/falsy bugs em validação (L1 root cause — procurar padrão `or DEFAULT` em outros pontos)
- Over-broad exception handlers
- Config drift entre deploy.sh e endpoint live (smell de automação parcial)

**Secondary:**
- Test determinism (seed randomização em `normalize_input` dificulta comparação direta — mock `random` em tests)

## Tasks / Subtasks

- [ ] **Task 1: Fix `serverless/deploy.sh` executionTimeoutMs (AC: 1)**
  - [ ] Alterar linha `executionTimeoutMs: 120000` → `executionTimeoutMs: 300000` no payload do POST /endpoints
  - [ ] Adicionar bloco "update if exists" ao caminho idempotente: se endpoint existente retorna valor diferente do target, chamar `PATCH /v1/endpoints/{id}` com novo valor
  - [ ] Parametrizar via env `EXECUTION_TIMEOUT_MS` com default 300000 (flexibilidade para futuro)
  - [ ] Rodar duas vezes `bash serverless/deploy.sh` e confirmar segunda execução reusa sem alterar config
  - [ ] Update `docs/usage/runpod-serverless-deploy.md` section "2. Criar template + endpoint" pra refletir novo valor

- [ ] **Task 2: Fix `normalize_input` zero-coercion bug (AC: 2)**
  - [ ] Substituir padrão `int(raw.get("steps") or DEFAULT_STEPS)` por:
    ```python
    v = raw.get("steps")
    steps = DEFAULT_STEPS if v is None else int(v)
    ```
  - [ ] Aplicar mesma correção a `width`, `height`
  - [ ] Preservar validações de range existentes (elas vão rejeitar `0` naturalmente após a mudança)
  - [ ] Garantir que `seed` continua funcionando: `None` → random, `""` → random, `0` → seed=0 válido (não default)

- [ ] **Task 3: Narrow exception scope em `wait_for_comfy` (AC: 3)**
  - [ ] Em `serverless/handler.py:63-73`, trocar `except Exception as e:` por `except (urllib.error.URLError, ConnectionError, OSError, TimeoutError) as e:`
  - [ ] Verificar que não há outras capturas amplas de `Exception` no handler (grep) — documentar se encontradas

- [ ] **Task 4: Align `COMFY_GENERATION_TIMEOUT_S` default (AC: 4)**
  - [ ] Mudar `COMFY_GENERATION_TIMEOUT_S = int(os.environ.get("COMFY_GENERATION_TIMEOUT_S", "120"))` para default `"280"`
  - [ ] Adicionar comment inline: `# Aligned with RunPod executionTimeoutMs (300s) minus 20s safety margin`

- [ ] **Task 5: Harden `poll_history` para status desconhecidos (AC: 5)**
  - [ ] Em `handler.py:128-144`, após checar `completed` e `status_str == "error"`, adicionar:
    ```python
    status_str = entry.get("status", {}).get("status_str")
    if status_str and status_str not in ("running", "success", "error"):
        log("warn", "unknown_status", prompt_id=prompt_id, status_str=status_str)
    ```
  - [ ] Continuar polling após log (não raise)

- [ ] **Task 6: Unit tests `serverless/tests/test_normalize_input.py` (AC: 6)**
  - [ ] Criar arquivo usando pytest
  - [ ] Implementar 13 casos listados em AC6
  - [ ] Para casos de seed aleatório, verificar apenas que retorna int em range válido (não valor específico)
  - [ ] `pytest serverless/tests/test_normalize_input.py -v` deve passar 13/13

- [ ] **Task 7: Regressão warm bench (AC: 7)**
  - [ ] Rodar `python3 serverless/tests/latency_bench.py --warm 20 --cold 0`
  - [ ] Capturar p50, p95, p99; confirmar p95 ≤ 8.5s
  - [ ] Salvar output em `serverless/tests/bench-results-2.1.1-regression-{ts}.json`
  - [ ] Documentar métricas em Dev Agent Record

- [ ] **Task 8: Update story Dev Agent Record + File List + Change Log**
  - [ ] Marcar todos os checkboxes [x]
  - [ ] File List: `serverless/handler.py`, `serverless/deploy.sh`, `serverless/tests/test_normalize_input.py` (novo), `docs/usage/runpod-serverless-deploy.md`
  - [ ] Completion Notes com links para commits individuais (se split)
  - [ ] Status: Draft → Ready for Review

## Dev Notes

### Source Artifacts (verified)

- **QA Results Story 2.1:** `docs/stories/2.1.runpod-serverless-flux-endpoint.story.md` seção "QA Results" (M2, L1-L5)
- **Commit sendo corrigido:** `9e55f58` (feat: implement Story 2.1)
- **Epic context:** `docs/prd/epic-2-consumer-integration.md` — Story 2.1.1 não altera Epic scope, apenas hardens Story 2.1 output

### Arquivos-alvo e linhas específicas

| Arquivo | Linhas aprox | Task |
|---|---|---|
| `serverless/deploy.sh` | 90 | T1 |
| `serverless/handler.py` | 30, 63-73, 128-144, 167-184 | T2, T3, T4, T5 |
| `serverless/tests/test_normalize_input.py` | (new file) | T6 |
| `docs/usage/runpod-serverless-deploy.md` | section "2. Criar template" | T1 |

### Dependências

- **Bloqueia:** nada explicitamente. Story 2.2 (TS SDK) pode começar em paralelo se aceitar risco dos bugs permanecerem durante seu dev, mas é preferível fechar 2.1.1 antes porque L1 (zero-coercion) pode confundir consumers TS que passarem `steps: 0` experimentalmente.
- **Bloqueada por:** nada — todos os artefatos e infra existem.
- **Relacionada:** Story 2.1.2 (architectural decision M1 cold latency) — independente, pode rodar em paralelo.

### Key Constraints

- **Zero-regression no warm:** após mudanças, warm p95 deve ficar ≤ 8.5s (baseline 7.0s em 2.1 com 20% margem). Se regredir, investigar e **não mergear**.
- **Idempotência do deploy.sh:** double-execution sem side effects é testable via `diff .env_before .env_after`.
- **Sem mudanças de API pública:** handler schema (AC1 de 2.1) não muda; apenas validação fica mais estrita.
- **Sem novas dependências:** `pytest` já comum em Python devs mas NÃO está em `requirements.txt` ainda — adicionar como dev-dep (ex: `requirements-dev.txt` opcional, ou pip install ad-hoc). Decisão fica com @dev; se não houver `requirements-dev.txt`, criar.

### IDS Check (REUSE > ADAPT > CREATE)

- **REUSE:** `latency_bench.py` existente (Task 7)
- **ADAPT:** `handler.py` (L1, L2, L4) e `deploy.sh` (M2) — todas as mudanças <5% do código existente
- **CREATE:** `test_normalize_input.py` (novo, inevitável para L5); `requirements-dev.txt` se adotado

### Risks

| Risk | Mitigation |
|---|---|
| Warm regride após mudanças | Bench regression obrigatório (AC7); rollback simples via git revert se falhar |
| PATCH em endpoint existente causa downtime breve | RunPod PATCH faz rolling release; testar em bench ~30s depois |
| `normalize_input` mudança quebra consumer que passava `0` intencionalmente | Consumer não deveria passar 0 (violaria range de steps/dims); se fizer, erro explícito é melhor que default silencioso |
| pytest não instalado em ambiente dev | `pip install --break-system-packages pytest` ou `python -m venv` — dev escolhe; adicionar nota em README |

### Testing

- **Test location:** `serverless/tests/test_normalize_input.py` (novo); `serverless/tests/latency_bench.py` (existente)
- **Test framework:** pytest para unit; script ad-hoc para regression bench
- **Specific requirements:**
  - Unit tests devem passar 13/13 (AC6)
  - Regression bench warm p95 ≤ 8.5s (AC7)
  - Deploy.sh idempotência validada (AC1) — manual via `git diff .env` após 2 execuções
  - Nenhum smoke test de endpoint necessário — handler.py unit-testável para o escopo desta story (sem tocar em workflow/inference paths)

## Change Log

| Date | Version | Description | Author |
|---|---|---|---|
| 2026-04-21 | 0.1 | Story drafted from Story 2.1 QA Results (M2 + L1-L5). Scope: pure dev fixes; architectural decision M1 deferida para 2.1.2 | @sm (River) |
| 2026-04-21 | 0.2 | PO validation passed (10/10 — GO incondicional). Quality trend: score máximo explicado pela referência direta ao QA Results (evidência pré-existente), escopo bounded e testável objetivamente, zero ambiguidade arquitetural (M1 properly deferred). Status Draft→Ready | @po (Pax) |

## Dev Agent Record

*A ser preenchido pelo @dev durante implementação.*

### Agent Model Used
TBD

### Debug Log References
TBD

### Completion Notes List
TBD

### File List
TBD

## QA Results

*A ser preenchido pelo @qa após review.*
