.env
.env.*
!.env.example

# Environment & Secrets (AIOX)
.env.local
.env.*.local
*.key
*.pem

# Dependencies (AIOX)
node_modules/
node_modules

# Build & Logs (AIOX)
dist/
build/
*.log
logs/

# IDE & OS (AIOX)
.DS_Store
Thumbs.db
.idea/
*.swp

# AIOX Local (AIOX)
.aiox-core/local/
.claude/settings.local.json
.aiox/install-log.txt

# Handoff artifacts (runtime)
.aiox/handoffs/

# AIOX framework (project-dev mode)
.aiox-core/

# Windows NTFS metadata (alt data streams in WSL)
*Zone.Identifier*

# Serverless test artifacts (generated, not checked-in)
serverless/tests/__pycache__/
serverless/tests/smoke-*.png
serverless/tests/bench-results-*.json
__pycache__/

# Docker smoke test mount residue (AC7 external onboarding validation)
test-onboarding/

# Vercel project link metadata (local-only; see web/landing/README.md)
.vercel/

# User-local experiments (unrelated to servegate)
contabhub_design_system.html

# AIOX framework artifacts (local-only, not for public repo consumers)
# Owner workflow tooling — not relevant to external SDK/API users
.ai/
.agent/
.agents/
.antigravity/
.claude/
.codex/
.cursor/
.gemini/
.github/agents/
squads/
.env.example
AGENTS.md
.aiox/notes/story-4.2/deploy-record-*.md
.aiox/notes/story-4.2/ac11-smoke-evidence-*.md
